## 1. Fichiers i18n français

- [x] 1.1 `messages/fr.json` ligne 10 : remplacer `19 ANS` par `+ DE 10 ANS` dans `hero.eyebrow`
- [x] 1.2 `messages/fr.json` ligne 24 : remplacer `19 ANS` par `+ DE 10 ANS` dans `pitchPlayground.eyebrow`
- [x] 1.3 `messages/fr.json` ligne 27 : remplacer `"19"` par `"+ de 10"` dans `pitchPlayground.metric1Value`

## 2. Fichiers i18n anglais

- [x] 2.1 `messages/en.json` ligne 10 : remplacer `19 YEARS` par `10+ YEARS` dans `hero.eyebrow`
- [x] 2.2 `messages/en.json` ligne 24 : remplacer `19 YEARS` par `10+ YEARS` dans `pitchPlayground.eyebrow`
- [x] 2.3 `messages/en.json` ligne 27 : remplacer `"19"` par `"10+"` dans `pitchPlayground.metric1Value`
- [x] 2.4 `messages/en.json` ligne 59 : remplacer `19 years` par `10+ years` dans `about.title`
- [x] 2.5 `messages/en.json` ligne 60 : remplacer `After 19 years` par `After 10+ years` dans `about.bio`
- [x] 2.6 `messages/en.json` ligne 93 : remplacer `19 years` par `10+ years` dans `aboutPage.meta.description`
- [x] 2.7 `messages/en.json` ligne 97 : remplacer `19 years` par `10+ years` dans `aboutPage.hero.headline`
- [x] 2.8 `messages/en.json` ligne 105 : remplacer `For 19 years` par `For 10+ years` dans `aboutPage.pivot.body1`
- [x] 2.9 `messages/en.json` ligne 114 : remplacer `19 years` par `10+ years` dans `aboutPage.timeline.title`

## 3. Composant React et tests

- [x] 3.1 `src/components/sections/Hero.tsx` ligne 47 : remplacer `value: "19"` par `value: "+ de 10"`
- [x] 3.2 `e2e/about.spec.ts` ligne 29 : mettre à jour le commentaire pour refléter la nouvelle valeur
